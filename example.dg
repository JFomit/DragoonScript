fn ($) f x = f x
fn (|>) x f = f x

fn main () =
    print "Enter the url:"
    let url = read ()

    let result = (shell"curl" <<< "-s" <<< url) |> run
    printline $ if result == 0 then "Ok!" else "Error: " ++ result
